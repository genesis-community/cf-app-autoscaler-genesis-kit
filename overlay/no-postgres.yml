instance_groups:
  - (( delete "postgres" ))
  - (( delete "postgres_autoscaler" ))

releases:
  - (( delete "postgres" ))

variables:
  - (( delete "postgres_ca" ))
  - (( delete "postgres_server" ))
  - (( delete "postgres_client" ))
