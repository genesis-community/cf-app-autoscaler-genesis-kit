# CAs should last 10 years instead of the default Credhub 1y
variables:
- { name: app_autoscaler_ca_cert,  options: { duration: 3650 } }
- { name: postgres_ca,             options: { duration: 3650 } }
