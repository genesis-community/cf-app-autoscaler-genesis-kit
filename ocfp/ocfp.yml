---
meta:
  ocfp:
    vault:
      config_prefix: (( grab params.ocfp_vault_config_prefix || "config/" ))
      config:        (( concat genesis.ocfp_config_mount genesis.ocfp_env ))

    azs:
      - (( concat genesis.env "-z1" )) # FIXME: Should just be the base name
      - (( concat genesis.env "-z2" ))
      - (( concat genesis.env "-z3" ))

    networks:
      - name: (( concat genesis.env "." genesis.type ".net-autoscaler"       ))

    disk_types:
      postgres:         (( concat genesis.env "." genesis.type ".disk-as-postgres"       ))

    vm_types:
      apiserver:        (( concat genesis.env "." genesis.type ".vm-as-apiserver"        ))
      scalingengine:    (( concat genesis.env "." genesis.type ".vm-as-scalingengine"    ))
      scheduler:        (( concat genesis.env "." genesis.type ".vm-as-scheduler"        ))
      operator:         (( concat genesis.env "." genesis.type ".vm-as-operator"         ))
      eventgenerator:   (( concat genesis.env "." genesis.type ".vm-as-eventgenerator"   ))
      metricsforwarder: (( concat genesis.env "." genesis.type ".vm-as-metricsforwarder" ))
      postgres:         (( concat genesis.env "." genesis.type ".vm-as-postgres"         ))

exodus: # This should be taken from the manifest, not constructed here (might already be done in base)
  autoscaler_api_domain:     (( concat "autoscaler." params.cf_system_domain ))
  autoscaler_metrics_domain: (( concat "autoscalermetrics." params.cf_system_domain ))
  service_broker_domain:     (( concat "autoscalerservicebroker." params.cf_system_domain ))

params:
  network:  (( grab meta.ocfp.networks[0].name ))

# Instance group OCFP overrides
instance_groups:
  - name: apiserver
    azs: (( grab meta.ocfp.azs ))
    networks: (( grab meta.ocfp.networks ))
    vm_type: (( grab meta.ocfp.vm_types.apiserver ))
    stemcell: default
  - name: scalingengine
    azs: (( grab meta.ocfp.azs ))
    networks: (( grab meta.ocfp.networks ))
    vm_type: (( grab meta.ocfp.vm_types.scalingengine ))
    stemcell: default
  - name: scheduler
    azs: (( grab meta.ocfp.azs ))
    networks: (( grab meta.ocfp.networks ))
    vm_type: (( grab meta.ocfp.vm_types.scheduler ))
    stemcell: default
  - name: operator
    azs: (( grab meta.ocfp.azs ))
    networks: (( grab meta.ocfp.networks ))
    vm_type: (( grab meta.ocfp.vm_types.operator ))
    stemcell: default
  - name: eventgenerator
    azs: (( grab meta.ocfp.azs ))
    networks: (( grab meta.ocfp.networks ))
    vm_type: (( grab meta.ocfp.vm_types.eventgenerator ))
    stemcell: default
  - name: metricsforwarder
    azs: (( grab meta.ocfp.azs ))
    networks: (( grab meta.ocfp.networks ))
    vm_type: (( grab meta.ocfp.vm_types.metricsforwarder ))
    stemcell: default
  - name: postgres
    azs: (( grab meta.ocfp.azs ))
    networks: (( grab meta.ocfp.networks ))
    vm_type: (( grab meta.ocfp.vm_types.postgres ))
    persistent_disk_type: (( grab meta.ocfp.disk_types.postgres ))
    stemcell: default
